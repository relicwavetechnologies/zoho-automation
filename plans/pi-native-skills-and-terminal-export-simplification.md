# Pi-native skills and terminal workflows

> Superseded execution plan, reduced to a current architecture pointer on
> 2026-08-11. Git preserves the completed rollout history.

The implementation is live:

```text
authorized DB skills -> runtime-owned SKILL.md files -> Pi native skill loader

user request -> source/destination skills -> one persistent Python file
             -> credential-free divo-local -> governed typed backend tools
             -> connected destination -> read-back verification
```

The old candidate/offer/sample export planner is removed and is not a fallback.

Use these active plans:

- `plans/cloud-pi-runtime-optimization-consistency-and-proof.md`
- `plans/cloud-pi-skills-rewrite-and-db-rollout.md`
- `plans/cloud-pi-production-tool-e2e-tracker.md`
