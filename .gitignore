ottomator-agents/

# OS / dependency noise
.DS_Store
**/.DS_Store
**/node_modules/
.pnpm-store/
*.tsbuildinfo
**/*.tsbuildinfo
pi-bridge/
t3-code/
advance-backend/pnpm-workspace.yaml

# graphify knowledge graph (local only, never commit)
graphify-out/
.agent-seat/

# External reference codebases used for local research only
research/
research/hermes-agent/
research/graphiti/
openworker/

# production/local secrets
.env
.env.local
.env.production
.env.production.local
.env.*.local
.context/secrets/

# Local desktop release output and updater signing material.
jan/local-dmg/
jan/release-output/
.scratch/

# Local runtime, downloaded evidence, and planning artifacts.
.commandcode/
advance-backend/.data/
advance-backend/latest-agent-run.log
lark-im-resources/
lark-search-message-auth.png
plan-divo-*.html

# Local-only handover/context notes. Never committed.
*.local.md

# Local dev stack pids and per-service logs.
.dev-stack/

# Scratch and captured run data. These hold real Lark transcripts, customer
# records and colleague addresses; a commit that sweeps them in writes that into
# history permanently. Nothing here is source.
tmp/
output/
.divo-state/

# Generated investigation and review pages that land at the repo root.
/px.html
/research-*.html
/review-*.html

# Local reference clone (not part of Divo)
whatsapp-followup-agent/

# Runtime pids written by scripts/dev-stack.sh
.context/

# Scratch prototypes, same as the research-/review- ones above
/plan-*.html
/proto-*.html
