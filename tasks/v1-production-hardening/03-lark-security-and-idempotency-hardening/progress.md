# Progress Log — Lark Security and Idempotency Hardening

- 2026-03-05 00:55:08 IST | actor: codex | Initialized task folder, context, todo checklist, and baseline verification gates. | blockers: none | next: claim owner in todo.md and start implementation work.
- 2026-03-05 02:08:49 IST | actor: codex | Implemented strict webhook trust + idempotency hardening: (1) verifier now enforces signature-required mode when `LARK_WEBHOOK_SIGNING_SECRET` is configured, adds explicit `signature_required` and `invalid_timestamp` reasons, and keeps token-mode only when signing secret is absent; (2) webhook route now maps verifier failures (`401` for auth/config/header/signature/token/timestamp failures, `403` for replay-window), uses event-id-first dedupe with message-id fallback, adds best-effort message alias claim when event-id is primary, and fail-closes idempotency store outages with `503` + `lark.webhook.idempotency.unavailable`; (3) idempotency repository now supports channel/key-type/key claims with compatibility wrapper. Added automated coverage: `backend/tests/lark-webhook-verifier.test.cjs` expanded for signature/timestamp/replay scenarios, and new `backend/tests/lark-webhook-security-idempotency.test.cjs` for route mapping + dedupe + unavailable-store behavior. Validation evidence: `pnpm -C backend build` (pass), `pnpm -C backend test:unit:lark` (pass, 33/33). | blockers: manual verification not executed in this session; needs running backend + Redis + live/synthetic webhook calls to validate HTTP status/duplicate/503 behavior over actual network ingress. | next: run manual checklist on live backend (valid duplicate payload twice, signature-required 401, stale timestamp 403, Redis-down 503), capture pass/fail in this file, and then mark manual todo done.
