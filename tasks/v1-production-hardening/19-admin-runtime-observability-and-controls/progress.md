# Progress Log — Admin Runtime Observability and Controls

- 2026-03-05 00:55:08 IST | actor: codex | Initialized task folder, context, todo checklist, and baseline verification gates. | blockers: none | next: claim owner in todo.md and start implementation work.
- 2026-03-05 12:00:51 IST | actor: codex | Claimed ownership and started Task 19 implementation. Baseline reviewed for admin runtime API/routes, runtime task store, worker snapshots, and Controls UI for additive health + scoped visibility changes. | blockers: none | next: implement backend runtime health endpoint and scoped task filtering before UI update.
- 2026-03-05 12:09:16 IST | actor: codex | Implemented Task 19 deliverables: added `GET /api/admin/runtime/health`, added company scoping enforcement in runtime list/get/trace/control/recover service paths, extended runtime snapshot with `companyId/scopeVisibility`, propagated company context from `zoho-read` agent results, and upgraded Controls page with runtime health panel + scope/engine metadata. Added automated tests: `admin-runtime.health`, `admin-runtime.scope`, `admin-runtime.authz`. Commands: `pnpm -C backend build` (pass), `node --test backend/tests/admin-runtime.health.test.cjs backend/tests/admin-runtime.scope.test.cjs backend/tests/admin-runtime.authz.test.cjs` (pass). | blockers: manual verification requires authenticated company-admin UI session against running backend to validate live control/recover and health panel flows. | next: start Task 20 by introducing tracing sink/redaction modules and boundary event emission.
- 2026-03-05 14:23:22 IST | actor: codex | Added company-admin control-plane endpoints for MCP rollout operations: `POST /api/admin/company/onboarding/lark-binding` (explicit tenant->company mapping) and `GET /api/admin/company/onboarding/provider-status` (active provider mode + health + capabilities). This closes the tenant-mapping loop required for scoped Lark execution. Commands: `pnpm -C backend build` (pass), `pnpm -C backend test:unit:lark` (pass), `pnpm -C backend test:unit:zoho` (pass). | blockers: manual UI/API verification still requires authenticated company-admin session and live tenant keys. | next: exercise new onboarding binding/provider-status routes from admin dashboard or API client and confirm webhook scoping behavior.
- 2026-03-05 15:45:36 IST | actor: codex | Re-ran admin runtime control-plane automated coverage after latest backend refinements. Command: `node --test backend/tests/admin-runtime.health.test.cjs backend/tests/admin-runtime.scope.test.cjs backend/tests/admin-runtime.authz.test.cjs` (pass). Confirmed scoped visibility for company-admin, unresolved task filtering, and authz enforcement for control/recover paths remain intact. | blockers: manual company-admin UI exercise against live backend session still pending. | next: execute ControlsPage manual pass with real company-admin token/session and capture screenshots/log evidence.
