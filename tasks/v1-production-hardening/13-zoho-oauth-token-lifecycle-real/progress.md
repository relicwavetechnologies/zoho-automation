# Progress Log — Zoho OAuth Token Lifecycle (Real)

- 2026-03-05 00:55:08 IST | actor: codex | Initialized task folder, context, todo checklist, and baseline verification gates. | blockers: none | next: claim owner in todo.md and start implementation work.
- 2026-03-05 11:20:00 IST | actor: codex | Claimed ownership and started Task 13 implementation with baseline build/unit snapshots captured before edits. | blockers: none | next: implement real Zoho OAuth token exchange, encrypted token persistence, and refresh lifecycle.
- 2026-03-05 11:45:28 IST | actor: codex | Implemented real Zoho OAuth lifecycle: added token HTTP client with bounded retry/backoff, AES-256-GCM token encryption helpers, token service (`exchangeAuthorizationCode/getValidAccessToken/forceRefresh`), onboarding connect integration, and additive `ZohoConnection` token metadata fields in Prisma. Added unit tests for exchange/refresh/retry/encryption and 401 refresh retry path. Commands: `pnpm -C backend build` (pass), `pnpm -C backend test:unit:zoho` (pass), `pnpm -C backend test:unit:lark` (pass), `pnpm -C backend test:unit:v1` (pass). | blockers: manual live OAuth verification requires real Zoho auth code exchange from configured tenant with `ZOHO_CLIENT_ID/ZOHO_CLIENT_SECRET/ZOHO_REDIRECT_URI/ZOHO_TOKEN_ENCRYPTION_KEY`. | next: run onboarding connect in live env and verify encrypted token persistence + refresh timestamps in `ZohoConnection`.
- 2026-03-05 14:23:22 IST | actor: codex | Extended connect flow to support `mode=mcp` securely: added MCP tool-discovery validation during connect, encrypted per-company MCP API key storage, provider-mode persistence on `ZohoConnection`, and additive provider metadata in connection DTO/status. Also added admin onboarding connect typing for `rest|mcp`. Commands: `pnpm -C backend build` (pass), `pnpm -C backend test:unit:zoho` (pass). | blockers: manual MCP connection verification still requires live company onboarding request with valid MCP endpoint/key. | next: execute live `POST /api/admin/company/onboarding/connect` in `mode=mcp` and verify encrypted MCP fields + provider metadata in onboarding status.
- 2026-03-05 14:49:05 IST | actor: codex | Applied Prisma schema to live DB with `pnpm prisma db push --accept-data-loss` so new Zoho/MCP columns and binding tables are available at runtime (previous runtime showed unknown-field drift). | blockers: none | next: validate onboarding `mode=mcp` persistence against newly synced schema.
