# Progress Log — Engine Switch and Rollback Contract

- 2026-03-05 00:55:08 IST | actor: codex | Initialized task folder, context, todo checklist, and baseline verification gates. | blockers: none | next: claim owner in todo.md and start implementation work.
- 2026-03-05 09:17:01 IST | actor: codex | Implemented deterministic engine switch + rollback contract hardening. Added explicit engine policy + rollback eligibility classifier in orchestration engine boundary, restricted fallback to eligible LangGraph failures, and added structured selection/eligible/executed/skipped rollback logs with reason codes. Propagated configured vs effective engine metadata through worker runtime task updates and exposed rollback metadata in admin runtime list/get/trace responses while preserving backward-compatible `engine` field semantics. Added additive runtime task fields (`configuredEngine`, `engineUsed`, `rolledBackFrom`, `rollbackReasonCode`) and documented rollback policy semantics in `ENV_REQUIREMENTS.MD`. Added automated tests: `tests/orchestration-engine-switch.test.cjs` and `tests/orchestration-worker.engine-metadata.test.cjs`. Automated evidence: `pnpm -C backend build` (pass), `pnpm -C backend test:unit:lark` (pass), `node --test tests/orchestration-engine-*.test.cjs tests/orchestration-worker.engine-*.test.cjs` from `backend/` (pass). Manual evidence: validated runtime behavior matrix via test-backed simulation: configured `legacy` stays legacy; configured `langgraph` success stays langgraph; eligible langgraph failure rolls back when rollback flag enabled; same failure does not rollback when flag disabled; non-eligible (HTTP 401) failure does not rollback. | blockers: none | next: proceed to Task 07 route-node determinism and keep new engine-switch tests in regression gate.
