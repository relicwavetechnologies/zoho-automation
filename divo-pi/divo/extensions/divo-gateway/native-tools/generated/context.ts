/* This file is generated by advance-backend/scripts/generate-pi-native-tools.ts. */
/* Edit the canonical backend Tool contract, then regenerate. */

import type { NativeToolSpec } from "../catalogue-contract.ts";

export const CONTEXT_NATIVE_TOOLS = [
  {
    "toolId": "webSearch",
    "name": "divo_web_search",
    "family": "context",
    "label": "Divo Web Search",
    "description": "Search the web for current information.",
    "promptSnippet": "Use divo_web_search for governed context work. The backend remains authoritative for access, connections, approvals, and execution.",
    "promptGuidelines": [
      "query: Search query",
      "limit: Max results (default 5)"
    ],
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "minLength": 1
        },
        "limit": {
          "type": "integer",
          "minimum": 1,
          "maximum": 10
        }
      },
      "required": [
        "query"
      ],
      "additionalProperties": false
    },
    "executionMode": "parallel"
  }
] as const satisfies readonly NativeToolSpec[];
