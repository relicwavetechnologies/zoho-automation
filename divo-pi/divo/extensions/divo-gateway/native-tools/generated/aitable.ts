/* This file is generated by advance-backend/scripts/generate-pi-native-tools.ts. */
/* Edit the canonical backend Tool contract, then regenerate. */

import type { NativeToolSpec } from "../catalogue-contract.ts";

export const AITABLE_NATIVE_TOOLS = [
  {
    "toolId": "aitableDatasheets",
    "name": "divo_aitable_datasheets",
    "family": "aitable",
    "label": "Divo Aitable Datasheets",
    "description": "Browse AITable workspaces and datasheets, and read, create, update or delete the records inside them.",
    "promptSnippet": "Use divo_aitable_datasheets for governed aitable work. The backend remains authoritative for access, connections, approvals, and execution.",
    "promptGuidelines": [
      "connectionId: reuse the exact run-bootstrap AITable account when supplied. Only list_spaces may omit it. operation: one of list_spaces|search_nodes|get_node|get_fields|list_views|list_records|create_records|update_records|delete_records. input: the arguments for that operation. Read get_fields before writing — it reports which fields are writable and what each accepts. The selected Divo connection authenticates the request with its stored AITable API key. AITable applies the key owner's own workspace permissions on top of Divo RBAC, so never send a key, token, or identity field in tool input."
    ],
    "parameters": {
      "type": "object",
      "properties": {
        "connectionId": {
          "type": "string",
          "format": "uuid"
        },
        "operation": {
          "type": "string",
          "minLength": 1
        },
        "input": {
          "type": "object",
          "additionalProperties": {}
        }
      },
      "required": [
        "operation"
      ],
      "additionalProperties": false
    },
    "executionMode": "sequential"
  },
  {
    "toolId": "aitableFields",
    "name": "divo_aitable_fields",
    "family": "aitable",
    "label": "Divo Aitable Fields",
    "description": "Inspect the field schema of an AITable datasheet, and add or remove fields. Removing a field deletes its data.",
    "promptSnippet": "Use divo_aitable_fields for governed aitable work. The backend remains authoritative for access, connections, approvals, and execution.",
    "promptGuidelines": [
      "connectionId: reuse the exact run-bootstrap AITable account when supplied. Only list_spaces may omit it. operation: one of list_spaces|search_nodes|get_node|get_fields|create_field|delete_field. input: the arguments for that operation. Read get_fields before writing — it reports which fields are writable and what each accepts. The selected Divo connection authenticates the request with its stored AITable API key. AITable applies the key owner's own workspace permissions on top of Divo RBAC, so never send a key, token, or identity field in tool input."
    ],
    "parameters": {
      "type": "object",
      "properties": {
        "connectionId": {
          "type": "string",
          "format": "uuid"
        },
        "operation": {
          "type": "string",
          "minLength": 1
        },
        "input": {
          "type": "object",
          "additionalProperties": {}
        }
      },
      "required": [
        "operation"
      ],
      "additionalProperties": false
    },
    "executionMode": "sequential"
  }
] as const satisfies readonly NativeToolSpec[];
