/* This file is generated by advance-backend/scripts/generate-pi-native-tools.ts. */
/* Edit the canonical backend Tool contract, then regenerate. */

import type { NativeToolSpec } from "../catalogue-contract.ts";

export const MENHOOD_NATIVE_TOOLS = [
  {
    "toolId": "menhoodData",
    "name": "divo_menhood_data",
    "family": "menhood",
    "label": "Divo Menhood Data",
    "description": "Query the company Menhood Airtable sync with a governed, read-only PostgreSQL SELECT.",
    "promptSnippet": "Use divo_menhood_data for governed menhood work. The backend remains authoritative for access, connections, approvals, and execution.",
    "promptGuidelines": [
      "sql: exactly one SELECT or read-only WITH query over menhood_orders, menhood_customers, menhood_products, or all_cities_with_pincode.",
      "parameters: positional JSON-safe values matching $1, $2, and so on; never interpolate user text into SQL.",
      "For row-level previews, include a deterministic ORDER BY on stable columns, e.g. order lines by order_date, order_number, id.",
      "menhood_advertisement_costs is intentionally unavailable."
    ],
    "parameters": {
      "type": "object",
      "properties": {
        "sql": {
          "type": "string",
          "minLength": 1
        },
        "parameters": {
          "type": "array",
          "items": {
            "anyOf": [
              {
                "type": "null"
              },
              {
                "type": "boolean"
              },
              {
                "type": "number"
              },
              {
                "type": "string",
                "maxLength": 4000
              }
            ]
          },
          "maxItems": 100
        }
      },
      "required": [
        "sql"
      ],
      "additionalProperties": false
    },
    "executionMode": "parallel"
  }
] as const satisfies readonly NativeToolSpec[];
