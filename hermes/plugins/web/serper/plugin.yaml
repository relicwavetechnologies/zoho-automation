name: web-serper
version: 1.0.0
description: "Serper (Google SERP) — web search via serper.dev's Google Search API. Requires SERPER_API_KEY (free tier ~2.5k queries/month at https://serper.dev). Returns organic results plus Google's answer box / knowledge graph."
author: NousResearch
kind: backend
provides_web_providers:
  - serper
